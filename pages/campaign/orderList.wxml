<view> 

      <view class="one-order" wx:for="{{orderList}}" wx:for-index="index" data-index="{{index}}" data-id="{{item.id}}" wx:key="{{item.id}}" data-id="{{item.id}}" bindtap="oneOrder"> 
        <include src="../common/orderTemp.wxml"></include>
      </view>

</view>
